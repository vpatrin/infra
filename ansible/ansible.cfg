[defaults]
inventory = inventory/hosts.ini
roles_path = roles
vault_password_file = ~/.ansible_vault_pass
# Disabled for DR bootstrap — fresh VPS has unknown host key.
host_key_checking = false
retry_files_enabled = false

[privilege_escalation]
become = true
become_method = sudo
